import { Container } from "reactstrap";

const Banner = () => {
    return (
        <Container fluid className="text-center py-5">
            banner
        </Container>
    );
};

export default Banner;
