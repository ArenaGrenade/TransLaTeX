{
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "express-rate-limit": "^5.2.3",
    "node-fetch": "^2.6.1"
  },
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "cd web && yarn && yarn run build"
  }
}
